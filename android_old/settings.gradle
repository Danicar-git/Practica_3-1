pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id "dev.flutter.flutter-plugin-loader" version "1.0.0"
}

include(":app")

// Carga la configuración de Flutter
apply from: file("../flutter/flutter_settings.gradle")